from django.urls import path
# from .views import PackageList, PackageDetail

app_name = 'api'

urlpatterns = [
    # path('<int:pk>/', PackageDetail.as_view(), name='packagedetail'),
    # path('', PackageList.as_view(), name='packagelist'),
]